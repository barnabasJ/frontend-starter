{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm build:server --watch\" \"nodemon --watch dist/server dist/server/index.js\"",
    "clean": "rimraf dist",
    "build": "pnpm clean && pnpm build:server && pnpm build:client",
    "build:client": "vite build",
    "build:server": "esbuild src/server/index.ts --platform=node --sourcemap=inline --target=node18.12 --packages=external --outdir=dist/server",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fastify/express": "^2.3.0",
    "@fastify/middie": "^8.1.0",
    "@types/node": "^18.11.18",
    "esbuild": "^0.16.11",
    "fastify": "^4.10.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.6.1"
  },
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.9",
    "@vitejs/plugin-react": "^3.0.0",
    "concurrently": "^7.6.0",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "typescript": "^4.9.3",
    "vite": "^4.0.0"
  }
}
